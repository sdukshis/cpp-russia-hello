before_script:
  - apt update && apt install g++-5 cmake python-pip
  - pip install conan
  - conan user

stages:
  - build
  - test

gcc-5:
  stage: build
  script:
    - conan test_package --build=missing
  tags:
    - docker
